@Library('jenkins-demo-library') _

pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                script {
                    def result = buildApp()
                    echo "Build result: ${result}"
                }
            }
        }
    }
}
